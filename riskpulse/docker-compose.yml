services:
  riskpulse:
    build: .
    ports:
      - "${PORT:-8090}:${PORT:-8090}"
    volumes:
      - ./data/source:/app/data/source:ro
    environment:
      SOURCE_DATA_DIR: /app/data/source
      PORT: ${PORT:-8090}
